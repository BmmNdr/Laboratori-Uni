<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>it.unimib.sd2026</groupId>
    <artifactId>hello-world</artifactId>
    <packaging>war</packaging>

    <version>0.1</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
        <jettyVersion>12.1.8</jettyVersion>
    </properties>
    
    <dependencies>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.1.0</version>
            <!-- La dipendenza serve per compilare il codice, ma non serve integrarla nel file WAR perché il server Jetty già la contiene. -->
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Questo plugin serve per integrare Jetty con Maven, per poter fare il deploy delle servlet in automatico sul server Web integrato. -->
            <plugin>
                <groupId>org.eclipse.jetty.ee11</groupId>
                <artifactId>jetty-ee11-maven-plugin</artifactId>
                <version>${jettyVersion}</version>
                <configuration>
                    <scan>1</scan>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
